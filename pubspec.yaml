name: eco_alerta_app
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.7.2


dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  image_picker: ^1.1.2

  sqflite: ^2.0.0+4
  
  path_provider: ^2.0.2

  shared_preferences: ^2.0.15


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0
  sqflite: ^2.3.2  # (ou a última versão estável)
  sqflite_common_ffi: ^2.3.5  # Desktop (Windows, Linux, macOS)
  flutter_launcher_icons: ^0.13.1
  path: any

flutter_icons:
  android: true
  ios: true
  image_path: "assets/images/logo_EcoAlerta.jpeg"

flutter:
  assets: 
    - assets/images/

  uses-material-design: true
